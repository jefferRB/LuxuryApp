@model LuxuryApp.Models.Productos.ProductoViewModel

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
<link rel="stylesheet" href="~/css/productos.css" asp-append-version="true">

<div class="kpi-card prod-form-shell">

    <!-- ── CABECERA ── -->
    <div class="prod-form-header">
        <div class="prod-brand-row">
            <div class="prod-brand-icon">
                <i class="bi bi-box-seam-fill"></i>
            </div>
            <div>
                <span class="prod-eyebrow">GESTIÓN DE PRODUCTOS</span>
                <h1 class="prod-title">Registrar Producto</h1>
            </div>
        </div>
        <hr class="prod-form-divider" />
    </div>

    <!-- ── CUERPO ── -->
    <div class="prod-form-body">

        <form asp-action="Create" method="post" id="formCrearProducto">

            @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.Any(v => v.Errors.Count > 0))
            {
                <div class="prod-validation-summary" role="alert">
                    <div asp-validation-summary="ModelOnly"></div>
                </div>
            }

            <partial name="_ProductoForm" model="Model" />

            <!-- Info Box: Control de Stock -->
            <div class="prod-info-box">
                <div class="prod-info-box-icon">
                    <i class="bi bi-shield-check"></i>
                </div>
                <div class="prod-info-box-body">
                    <div class="prod-info-box-title">Control de Stock</div>
                    <p class="prod-info-box-text">
                        El sistema mostrará una alerta cuando la <strong>cantidad en inventario</strong>
                        caiga por debajo del <strong>stock mínimo</strong> definido.
                        Esto te ayuda a reponer productos antes de quedarte sin existencias.
                    </p>
                </div>
            </div>

            <!-- Acciones -->
            <div class="prod-form-actions">
                <button type="submit" class="prod-btn prod-btn--primary">
                    <i class="bi bi-floppy2-fill"></i>
                    Guardar Producto
                </button>
                <a asp-action="Index" class="prod-btn prod-btn--secondary">
                    <i class="bi bi-arrow-left"></i>
                    Volver
                </a>
                <button type="reset" class="prod-btn prod-btn--ghost prod-form-actions-end">
                    <i class="bi bi-x-circle"></i>
                    Limpiar formulario
                </button>
            </div>

        </form>

    </div>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
